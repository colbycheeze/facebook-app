- if user_signed_in?
  %li= link_to 'Log out', destroy_user_session_path, method: :delete, data: {confirm: 'Are you sure?'}
  %li= link_to 'Edit User', edit_user_registration_path
  %li= "Pending Invites (#{current_user.pending_requests.count})"