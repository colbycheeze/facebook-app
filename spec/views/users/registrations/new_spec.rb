require 'rails_helper'

describe 'users/registrations/new.html.haml', :type => "view" do

 it "should have name, and email fields" do
  # expect(rendered).to have_content 'Name'
  # expect(rendered).to have_content 'Email'
 end
end